[project]
name = "pangolin"
version = "0.0.1"
description = "Probabilistic inference focused on fun"
requires-python = ">=3.11,<3.13"
dependencies = [
    "jax==0.4.38",
    "numpy",
    "scipy",
    "numpyro",
    "funsor>=0.4.5",
    "makefun>=1.16.0",
    "blackjax>=1.2.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "black>=25.1.0",
    "pytest>=8.4.1",
    "jupyter",
    "seaborn",
]

[project.optional-dependencies]
torch = ["torch","funtorch"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--doctest-modules"
